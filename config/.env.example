# DEBUG
#
# Enables or disables debug mode.
#
# Accepted values (parsed explicitly by the application):
#   true, false, 1, 0, yes, no, on, off
#
# Notes:
# - The value is always loaded as a string.
# - It MUST be parsed explicitly using FILTER_VALIDATE_BOOLEAN.
#
# Example:
#   DEBUG=true
#
DEBUG=false
